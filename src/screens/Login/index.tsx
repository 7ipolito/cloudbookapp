import React from 'react';
import { View, Text } from 'react-native';
import { Container, Title } from './styles';

export function Login(){
    return(
        <Container>
            <Title style={{fontFamily:'Acme_400Regular'}}>Cloudbook</Title>
        </Container>
    )
}